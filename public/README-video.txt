Drop your hero video here as:
  hero.mp4        — the looping background video for the homepage banner
  hero-poster.jpg — a still frame shown before the video loads / if it fails

components/Hero.tsx already references /hero.mp4 and /hero-poster.jpg,
so no code changes are needed once these files are in place.
