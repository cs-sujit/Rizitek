/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: "/Rizitek",
  assetPrefix: "/Rizitek/",
};

export default nextConfig;
